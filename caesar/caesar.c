#include <cs50.h>
#include <stdio.h>

int main(int argc, string argv[])
{
    // Check that there is one command-line argument.
    if(argc != 2)
    {
        printf("Usage: ./caesar Key\n");
        return 1;
    }
    else
    {
        printf("Success!\n");
    }

}