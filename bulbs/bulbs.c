#include <cs50.h>
#include <stdio.h>
#include <string.h>

const int BITS_IN_BYTE = 8;

void print_bulb(int bit);

int  main()
{
    int i, j, k;
    string input = get_string("Messege");

    for (int = 0; input[i] != '0'; i++)
    {
        string bit = input[i];
        for (j = 7; j+1>0; j++)
        {
    if (c>=(1<<j))
    {
        // Dark emoji
        printf("\U000026AB");
    }
    else 
    {
        // Light emoji
        printf("\U0001F7E1");
    }
}
